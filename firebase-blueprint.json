{
  "entities": {
    "User": {
      "title": "User",
      "description": "Information about the user's role and subscription",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user (wallet or guest ID)"
        },
        "walletAddress": {
          "type": ["string", "null"],
          "description": "The user's Solana wallet address"
        },
        "freeUsesCount": {
          "type": "integer",
          "description": "Number of free AI uses consumed"
        },
        "subscriptionExpiry": {
          "type": "string",
          "format": "date-time",
          "description": "ISO timestamp of when the subscription expires"
        }
      },
      "required": ["id", "freeUsesCount"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Collection of user data indexed by user ID"
    }
  }
}
